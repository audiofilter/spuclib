os:
  - osx
language: 
  - objective-c
compiler:
  - clang
before_install:
  - brew update
install:
  - brew install cmake
# Change this to your needs
before_script:
  - mkdir build
  - cd build
  - cmake ..
script: make
